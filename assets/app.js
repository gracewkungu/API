const currency = ['FB', 'AAPL', 'TSLA', 'GOOG'];

// displaycurrencyInfo function re-renders the HTML to display the appropriate content
const displaycurrenctyInfo = function () {

    const currency = $(this).attr('data-name');
    const queryURL = `https://min-api.cryptocompare.com/`;
     
}